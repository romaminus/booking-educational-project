{
  "name": "booking",
  "version": "1.0.0",
  "description": "hw project Booking",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start-server": "cd server && npm run start",
    "start-client": "cd client && npm run dev",
    "dev": "concurrently \"npm run start-server\" \"npm run start-client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "react-icons": "^5.5.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}